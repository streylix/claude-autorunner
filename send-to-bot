#!/bin/bash
# Helper script to send commands to the bot daemon

if [ $# -eq 0 ]; then
    echo "Usage: $0 <command>"
    echo "Example: $0 '!addmsg \"ls -la\" 1'"
    exit 1
fi

COMMAND_FILE="/tmp/claude-bot-commands"
echo "$1" > "$COMMAND_FILE"
echo "📤 Command sent to bot: $1"