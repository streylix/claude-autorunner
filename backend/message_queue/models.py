# Frontend-only queue system - No database models needed
# All message queue functionality is handled in the frontend
# This backend only provides pass-through endpoints

# Models removed to eliminate database persistence issues
# The frontend queue is now the single source of truth